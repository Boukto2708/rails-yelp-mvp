<%= simple_form_for [@restaurant, @review] do |f| %>
  <div class="form-inputs">
    <%= f.input :content %>

  </div>
  <%= f.input :rating %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
